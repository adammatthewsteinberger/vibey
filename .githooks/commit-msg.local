#!/bin/sh
# Chained from .githooks/commit-msg by vibey-gh: Conventional Commits.
command -v pre-commit >/dev/null 2>&1 || exit 0
exec pre-commit run --hook-stage commit-msg --commit-msg-filename "$1"

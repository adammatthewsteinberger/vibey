[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "vibey"
version = "0.2.0"
description = "A queue-based, six-phase conductor with optional visual design and Azure deployment."
readme = "README.md"
requires-python = ">=3.12"
license = { text = "MIT" }
dependencies = [
    "typer>=0.15",
    "asyncpg>=0.30",
    "structlog>=24.4",
    "textual>=0.86",
    "tomli-w>=1.1",
]

[project.scripts]
vibey = "vibey.cli.main:app"

[project.optional-dependencies]
# kopf pulls the whole Kubernetes client stack. Nobody running `vibey
# worker` on a laptop needs it, so the operator is an extra rather than a
# base dependency: `pip install vibey[operator]`.
operator = [
    "kopf>=1.37",
    "kubernetes>=31.0",
]
skills = [
    # Replace this source pin with vibey-skills>=2.18,<3 once the context
    # engine is available in a versioned wheel. The published 2.17.0 wheel
    # predates the index/packet CLI even though main still reports 2.17.0.
    "vibey-skills @ git+https://github.com/adammatthewsteinberger/vibey-skills.git@5c605684f544b528acb3d3e03808d04715a670e3",
]
dev = [
    # Release automation and the provenance guardrails, shared by every repo
    # in the family. Stdlib-only by design, so adopting it in five places
    # does not multiply anyone's dependency tree.
    "vibey-gh>=1.2",
    "pytest>=8.3",
    "pytest-asyncio>=0.24",
    "pytest-cov>=6.0",
    "pytest-xdist>=3.5",
    "hypothesis>=6.115",
    "ruff>=0.8",
    "mypy>=1.13",
    "import-linter>=2.1",
    "bandit>=1.8",
    "pip-audit>=2.7",
    "pre-commit>=4.0",
    # The operator ships in the same package and carries the same 100%
    # branch-coverage floor, so its dependencies must be present to test.
    "kopf>=1.37",
    "kubernetes>=31.0",
]

[tool.hatch.build.targets.wheel]
packages = ["src/vibey"]

[tool.hatch.metadata]
allow-direct-references = true

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
addopts = "-m 'not paid' -n auto --maxprocesses=8"
markers = [
    "integration: requires a running Postgres reachable via VIBEY_TEST_DATABASE_URL",
    "slow: takes more than a couple seconds to run",
    "system: delivery-stage-set end-to-end system test",
    "live: tests against real loop binaries (faked mode — scripted agents)",
    "paid: tests against real models (requires API keys, costs money)",
]

[tool.ruff]
line-length = 100
target-version = "py312"
include = ["src/**/*.py", "tests/**/*.py"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
# E501 only, and only because of the provenance header: an exact 230-character line
# compared byte-for-byte, unwrappable without failing the check that mandates it. Width on
# real code is still enforced by the formatter.
ignore = ["E501"]

[tool.mypy]
python_version = "3.12"
strict = true
packages = ["vibey"]
mypy_path = "src"

[[tool.mypy.overrides]]
module = "asyncpg.*"
ignore_missing_imports = true

[tool.coverage.run]
source = ["vibey"]
branch = true
parallel = true

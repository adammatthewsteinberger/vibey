# Superseded handoff note

This file was a session handoff note written after Milestones M0-M4, when
`bootstrap.py` was empty and no phase orchestration, CLI commands beyond
`--version`, or TUI existed yet. The project has progressed well past that
point since — `bootstrap.py` is wired, and CLI commands including
`vibey worker` and `vibey doctor --conformance` are implemented (see
`src/vibey/cli/main.py`).

It is kept only as a historical artifact of that session and is not linked
from any onboarding path. Do not treat it as a description of current
project status. For the current, accurate state of the project, see
`README.md`, `CLAUDE.md`, and `docs/index.md`.
